

const initialState = {
  'events': {},
  'groups': {},
  'people': {},
};

export default function apiData(state = initialState, action) {
  switch(action.type) {

    default:
      return state;
  }
}
