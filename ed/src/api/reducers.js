

const initialState = {
  'events': {},
  'groups': {},
  'people': {},
};

export function apiData(state = initialState, action) {
  switch(action.type) {

    default:
      return state;
  }
}

export function apiRequests(state = {}, action) {

}